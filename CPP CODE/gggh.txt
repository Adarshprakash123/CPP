1.% can’t be used with double or float.
2. A character or integer without a space could be the corresponding answer value. Pay attention to the capital and lowercase letters.
3.the addition of a character and an integer leads to an integer
4. !b evaluates to false (! operator negates the value, b is non-zero and hence !b becomes 0 which is false). 
5.#include<cmath> for round function- totalsalary= round ( basic + hra + da + allow - pf);
6.